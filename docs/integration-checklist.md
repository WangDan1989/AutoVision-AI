# AutoVision-AI 联调清单

## 基础连通

- [ ] `python scripts/check_runtime.py` 无阻断 FAIL
- [x] `backend` 可正常启动
- [ ] `frontend` 可正常启动
- [x] `GET /healthz` 返回 `ok`
- [ ] `/media/...` 能访问静态资源

## 数据层

- [ ] SQLite 文件自动创建
- [ ] `projects` 表可写入
- [ ] `task_queue` 表可查询

## 工作台

- [ ] 工作台首页可打开
- [ ] 五步页面可切换
- [ ] Toast 容器正常显示
- [ ] 失败任务区正常渲染

## 上传

- [ ] Mask 上传接口可成功写入 `storage/temp`
- [ ] 返回 `abs_path` 与 `relative_path`

## 后续真实能力接入

- [ ] Ollama 剧本拆解
- [ ] ComfyUI 首帧生成
- [ ] 视频工作流
- [ ] TTS 音频
- [ ] FFmpeg 合成导出

## Step 4 成功条件

- [ ] 已有至少一个已锁定首帧的分镜
- [ ] `ffmpeg -version` 可执行
- [ ] 单个分镜可成功生成 `.mp4` 片段
- [ ] 单个分镜失败时，页面能显示具体失败原因
- [ ] 批量视频只处理已锁帧分镜
- [ ] 批量音频只处理存在文本的分镜

## Step 5 成功条件

- [ ] 所有待导出分镜都已有视频片段
- [ ] 导出预检无阻断错误
- [ ] 可成功生成最终成片 `.mp4`
- [ ] 开启字幕时可成功烧录
- [ ] 开启转场时可成功输出
- [ ] “载入最近导出方案”可回填当前表单
- [ ] “一键补齐素材并导出”在缺锁帧时会先阻断提示

## 当前联调结论

- 当前仓库已修复后端冷启动时 `storage/` 未创建导致的启动失败
- 当前仓库默认要求在 `backend/.env` 中配置真实服务参数
- 当前建议使用 `Python 3.11/3.12/3.13` 运行后端，避免 `Python 3.14` 依赖安装失败
- 若未启动 `Ollama` 或 `ComfyUI`，后端现在会返回更明确的连接错误提示
